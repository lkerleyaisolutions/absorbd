# Runtime dependencies, pinned for reproducible builds (captured June 5, 2026).
# Python 3.13.

# Azure AI Foundry / agents (v2 SDK - uses AIProjectClient + Responses API)
azure-ai-projects==2.2.0
# Foundry IQ knowledge base + search index (KnowledgeBase/KnowledgeSource APIs)
azure-search-documents==12.0.0
# Content Safety groundedness checks (Safety agent)
azure-ai-contentsafety==1.0.0
# Entra ID auth for the runtime Foundry IQ MCP connection
azure-identity==1.25.3
azure-core==1.41.0
# OpenAI client returned by AIProjectClient.get_openai_client(); used directly
openai==2.41.0

# Backend
fastapi==0.136.3
uvicorn[standard]==0.49.0

# Ingestion / utilities
pdfplumber==0.11.9
beautifulsoup4==4.14.2
requests==2.34.2
python-dotenv==1.2.2

# Statistical correlation engine
scipy==1.17.1
statsmodels==0.14.6
